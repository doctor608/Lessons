#include <stdio.h>
#include <limits.h>

int main(void)
{
	char c = 65;
	
	float f;
	double d;

	short si;
	int i;
	long li;

	printf("%c%c%c\n", 76, 79, 76);

	//printf("size of si is %li\n", sizeof(si));
	//printf("size of i is %li\n", sizeof(i));
	//printf("size of li is %li\n", sizeof(li));

	//printf("SHRT_MAX is %i\n", SHRT_MAX);
	//printf("INT_MAX is %i\n", INT_MAX);
	//printf("LONG_MAX is %li\n", LONG_MAX);

	return 0;
}
