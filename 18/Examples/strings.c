#include <stdio.h>
#include <school.h>
#include <string.h>

int main(void)
{
        char string[] = { 'A', 'r', 't', 'y', 'o', 'm', '\0' };
        char string1[] = "Artyom";

        fgets(string, 32, stdin);
        getchar()
        putchar()
        printf()

        strlen()
        return 0;
}
