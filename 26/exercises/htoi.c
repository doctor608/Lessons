#include <stdio.h>
#include <stdlib.h>

_Bool is_valid_hex(const char* s);

int main(int argc, char* argv[])
{
        
        return EXIT_SUCCESS;
}

/* Returns 1 if 's' can be interpreted as a valid hexadecimal value. */
_Bool is_valid_hex(const char* s)
{
        int i;
        for (i = 0; *s != '\0'; ++i)
}
